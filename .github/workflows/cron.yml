name: call-render
on:
  schedule:
    - cron: '0 * * * *'   # 每整点 0 分
  workflow_dispatch:      # 手动触发按钮（必须）
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: curl /test
        run: curl -f https://xxx.onrender.com/test   # ← 换成你的域名
