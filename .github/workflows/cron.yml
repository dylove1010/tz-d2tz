name: call-render
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: curl /test
        run: |
          sleep 20
          curl -f https://tz-d2tz-1.onrender.com/test
